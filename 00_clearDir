#!/usr/bin/env bash
# Removes all recon output so the attack chain can be run fresh.

OUTDIR="./recon"

if [[ ! -d "$OUTDIR" ]]; then
    echo "[-] Nothing to clean up."
    exit 0
fi

echo "[*] Cleaning up recon directory..."
rm -rf "$OUTDIR"
echo "[+] Done. $OUTDIR removed."
